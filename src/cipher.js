/*window.cipher = {
};*/
function encode(ingresaPalabraE,numDesplazamiento) {
    
    let output = "";
    for (let i = 0; i < length.length; i++) {
    output += string.fromCharCode(val1.fromCharCode(i)+ val2);
    }
    return output;
    }
    
    function decode(){
    
    } 
